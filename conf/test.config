params {
  input      = "${projectDir}/data/in.fasta"
  outdir     = "${projectDir}/test_results"
  max_cpus   = 4
  max_memory = '16.GB'
  max_time   = '8.h'
}

process {
  cpus   = 1
  memory = '2.GB'
  time   = '1.h'
}
